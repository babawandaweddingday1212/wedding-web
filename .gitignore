# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 相簿原檔（82MB，57 張未壓縮的婚紗照）。
# 網站實際用的是 public/photos/album/ 裡壓縮並切好的版本，那些有進版控。
# 原檔只在重跑 `npm run album` 時需要，放進 git 會讓 repo 從 40MB 漲到 120MB
# 且無法回收，因此排除。要重新產生相簿時把原檔放回這個資料夾即可。
photos-src/album/

# 環境變數檔一律不進版控。
# 注意：這只防止「檔案被提交」，不代表放在裡面的值就安全 ——
# 任何 VITE_ 開頭的變數都會被編譯進前端 bundle，等同公開。
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
